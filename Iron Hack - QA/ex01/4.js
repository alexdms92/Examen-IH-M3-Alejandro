//@ts-check
/**
 * Esta funcion verifica si un numero es par
 * @param {number} n el numero a verificar
 * @returns {boolean} devuelve true si es par, false si es impar
 */

function esNumeroPar(n)
{
return n % 2 === 0;
}